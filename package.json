{
  "name": "cat-chatbot",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "heroku-postbuild": "cd backend && npm install && npm run build"
  }
}